spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: trip-database
      username: trip
      password: trip
  rabbitmq:
    host: ${RABBIT_MQ_HOST:localhost}
    virtualHost: ${RABBIT_MQ_VIRTUAL_HOST:}
    username: ${RABBIT_MQ_USERNAME:guest}
    password: ${RABBIT_MQ_PASSWORD:guest}
    template:
      reply-timeout: ${RABBIT_MQ_REPLY_TIMEOUT:3000}

geolocation-api:
  url: test
  api-key: test
